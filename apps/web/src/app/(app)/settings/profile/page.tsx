import { UnavailableSettings } from "@/components/UnavailableSettings";

export default function ProfileSettingsPage() {
  return <UnavailableSettings title="Firm Profile" description="Firm-profile editing is not available yet. Existing legal identity is kept unchanged." />;
}
