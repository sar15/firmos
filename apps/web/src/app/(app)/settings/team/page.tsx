import { UnavailableSettings } from "@/components/UnavailableSettings";

export default function TeamSettingsPage() {
  return <UnavailableSettings title="Team Members" description="Team invitations and role changes are not available in V1." />;
}
