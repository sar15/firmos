import { UnavailableSettings } from "@/components/UnavailableSettings";

export default function SecuritySettingsPage() {
  return <UnavailableSettings title="Security" description="Session management and account deletion are not available in V1." />;
}
